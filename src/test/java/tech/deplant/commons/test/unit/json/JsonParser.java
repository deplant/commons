package tech.deplant.commons.test.unit.json;

public class JsonParser {

	// new virtual thread on new branch

	// store strings in map
	// if strings are too large, store hashes

	// on new strings, check map
	// if new string, too large, check hash


}
